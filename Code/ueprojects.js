var root = document.querySelector(':root');
var Position = 0;


//ao invex de mover quest one tentar manter o tamanho da divisao mesmo sendo um tamanho de tela diferente
/*
function PrintScreenSize()
{
    let width = window.innerWidth;
    
    if (width < 843)
    {
        let OfSet = (843 - width) / 2;
        console.log (OfSet);
        document.getElementById('quest1').style.left = OfSet + 'px';
    }
    else
    {
        document.getElementById('quest1').style.left = 0 + 'px';
    }
}

setInterval(PrintScreenSize, 10);
*/